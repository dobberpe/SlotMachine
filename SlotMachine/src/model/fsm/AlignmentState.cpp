#include "AlignmentState.h"

std::unique_ptr<IState> AlignmentState::buttonPressed(SlotMachine& machine) {
	return nullptr;
}

std::unique_ptr<IState> AlignmentState::update(SlotMachine& machine) {
	// выравниваемся
	return nullptr;
}
